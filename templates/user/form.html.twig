{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-user" onclick="return confirm('Etes-vous sûr de vouloir modifier?')">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
