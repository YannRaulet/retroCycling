<form method="post" action="{{ path('admin_content_delete', {'id': article_content.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce contenu?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_content.id) }}">
    <button class="btn btn-danger admin-delete-btn">Supprimer</button>
</form>
