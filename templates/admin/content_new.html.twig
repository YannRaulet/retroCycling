{% extends 'admin/admin_layout.html.twig' %}

{% block title %}{{ parent() }}-Admin-Nouveau contenu{% endblock %}

{% block body %}

{% if background_pictures == false %}
    <section>
        <h1 class="title-crud">Créer un contenu</h1>
        <div class="page-form-edit">
            {{ include('admin/content_form.html.twig', { button_label: 'Créer'}) }}
            <a href="{{ path('admin_contents') }}">Retour aux contenus</a>
        </div>
    </section>
{% else %}

    {% for background_picture in background_pictures %}
        <section class="background" style="background-image:url( /assets/images/uploads/{{ background_picture.picture }}">
            <h1 class="title-crud">Créer un contenu</h1>
            <div class="page-form-edit">
                {{ include('admin/content_form.html.twig', { button_label: 'Créer'}) }}
                <a href="{{ path('admin_contents') }}">Retour aux contenus</a>
            </div>
        </section>
    {% endfor %}
{% endif %}

{% endblock %}